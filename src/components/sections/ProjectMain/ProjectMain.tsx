import clsx from 'clsx'

import s from './ProjectMain.module.scss'

const ProjectMain = () => {
  return <main className={clsx(s.main)}>Olá eu sou o Main!</main>
}

export default ProjectMain
